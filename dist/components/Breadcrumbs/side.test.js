"use strict";

var _react = require("@testing-library/react");

require("@testing-library/jest-dom");

var _ = require(".");

describe("<Menu />", () => {
  it("should render the logo", () => {// const {getByRole} = render(<SideMenu />)
    // expect(getByRole('img', { name: /crewhu logo/i })).toBeInTheDocument()
    // expect(getByRole('img', { name: /crewhu logo/i })).toHaveStyle({
    //   width: '100px',
    //   height: '62px'
    // })
  });
});